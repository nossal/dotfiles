export LANG=en_US.UTF-8
export DOTDIR=$HOME/.dotfiles
export ZSHDOTDIR=$DOTDIR/shell/zsh
export EDITOR=vi

WORK_FILE="$DOTDIR-work/env.sh"
if [[ -a $WORK_FILE ]]; then
    source $WORK_FILE
fi

source "$HOME/.cargo/env"
